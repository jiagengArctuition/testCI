name: filter  

on:
  workflow_dispatch:
  
  push:
    branches:
      - main1
    paths:
      - '**'
      - '!.gitignore'
      
jobs:
  filter:
    runs-on: macos-12
    steps:
      - run: echo "i'am running!"
